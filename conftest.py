# Presence of this conftest.py at the project root puts the root directory on
# sys.path, so tests under tests/ can `from tools import ...` and
# `from utils.data_loader import ...` when run with `pytest tests/`.
